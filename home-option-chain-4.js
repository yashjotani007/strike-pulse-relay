(()=>{const API='https://strike-pulse-relay.onrender.com/api/nifty-option-chain';const $=id=>document.getElementById(id);const fmt=(v,d=0)=>{const n=Number(v);return Number.isFinite(n)?n.toLocaleString('en-IN',{minimumFractionDigits:d,maximumFractionDigits:d}):'--'};const val=(o,k)=>{for(const x of k)if(o&&o[x]!=null)return o[x];return null};async function load(){const body=$('sp-chain-body');if(!body)return;try{const r=await fetch(API+'?symbol=NIFTY&t='+Date.now(),{cache:'no-store'});if(!r.ok)throw Error('HTTP '+r.status);const d=await r.json();const rows=(d.rows||[]).map(x=>({strike:val(x,['strike','strikePrice']),ce:x.ce||x.CE||{},pe:x.pe||x.PE||{}})).filter(x=>x.strike!=null).sort((a,b)=>a.strike-b.strike);const atm=Number(d.atmStrike??d.atm);let i=rows.findIndex(x=>Number(x.strike)===atm);if(i<0)i=rows.reduce((b,x,j)=>Math.abs(Number(x.strike)-atm)<Math.abs(Number(rows[b].strike)-atm)?j:b,0);const start=Math.max(0,Math.min(i-1,rows.length-4));const four=rows.slice(start,start+4);if($('sp-chain-spot'))$('sp-chain-spot').textContent=fmt(d.spot,2);if($('sp-chain-atm'))$('sp-chain-atm').textContent=fmt(atm);if($('sp-call-oi'))$('sp-call-oi').textContent=fmt(d.callOI);if($('sp-put-oi'))$('sp-put-oi').textContent=fmt(d.putOI);if($('sp-pcr'))$('sp-pcr').textContent=d.pcr==null?'--':Number(d.pcr).toFixed(2);if($('sp-max-pain'))$('sp-max-pain').textContent=fmt(d.maxPain);if($('sp-chain-expiry'))$('sp-chain-expiry').textContent=d.expiry||'--';body.innerHTML=four.map(x=>{const c=x.ce,p=x.pe,cc=Number(val(c,['oiChange','changeinOpenInterest']))||0,pc=Number(val(p,['oiChange','changeinOpenInterest']))||0;return `<tr><td>${fmt(val(c,['oi','openInterest']))}</td><td class="${cc>=0?'sp-up':'sp-down'}">${cc>=0?'+':''}${fmt(cc)}</td><td>${fmt(val(c,['ltp','lastPrice']),2)}</td><td class="sp-chain-strike">${fmt(x.strike)}</td><td>${fmt(val(p,['ltp','lastPrice']),2)}</td><td class="${pc>=0?'sp-up':'sp-down'}">${pc>=0?'+':''}${fmt(pc)}</td><td>${fmt(val(p,['oi','openInterest']))}</td></tr>`}).join('');const u=$('sp-chain-updated');if(u)u.textContent='Updated '+new Date().toLocaleTimeString('en-IN',{timeZone:'Asia/Kolkata',hour:'2-digit',minute:'2-digit',second:'2-digit'})+' IST';console.log('[Strike Pulse] Home 4-strike table updated',{atm,rows:four.map(x=>x.strike)});}catch(e){console.error('[Strike Pulse] Home option chain error',e)}}function start(){load();setInterval(load,30000)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start()})();