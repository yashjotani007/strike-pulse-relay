(function(){
'use strict';
const API='https://strike-pulse-relay.onrender.com/api/prices';
const VERSION='HOME-LIVE-STATUS-UPDATED-20260831-01';
console.log('%c🚀 STRIKE PULSE HOME PRICE JS LOADED — '+VERSION,'color:#00e676;font-weight:bold');
console.log('🌐 PRICE API:',API);
const markets={nifty:['nifty','nifty50'],banknifty:['banknifty','bankNifty'],finnifty:['finnifty','finNifty'],sensex:['sensex','SENSEX','bseSensex','bse_sensex'],vix:['vix','indiaVix','india_vix','INDIA_VIX']};
const fmt=v=>{const n=Number(v);return Number.isFinite(n)?n.toLocaleString('en-IN',{minimumFractionDigits:2,maximumFractionDigits:2}):'--'};
const clean=s=>(s||'').replace(/\s+/g,' ').trim().toUpperCase();
const istNow=()=>new Intl.DateTimeFormat('en-IN',{timeZone:'Asia/Kolkata',hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:false}).format(new Date());
function isMarketOpen(){const p=new Intl.DateTimeFormat('en-IN',{timeZone:'Asia/Kolkata',weekday:'short',hour:'2-digit',minute:'2-digit',hour12:false}).formatToParts(new Date());const x={};p.forEach(a=>{if(a.type!=='literal')x[a.type]=a.value});if(!['Mon','Tue','Wed','Thu','Fri'].includes(x.weekday))return false;const m=Number(x.hour)*60+Number(x.minute);return m>=555&&m<930;}
function updateMarketStatus(){const live=isMarketOpen();document.querySelectorAll('.sp-market-status').forEach(el=>{el.classList.remove('sp-live','sp-closed');if(live){el.classList.add('sp-live');el.textContent='LIVE';}else{el.classList.add('sp-closed');el.textContent='CLOSED';}});}
function diagnostics(){console.log('%c🔎 HOME JS DIAGNOSTICS','color:#42a5f5;font-weight:bold');console.log('📍 URL:',location.href);console.log('📦 .sp-market-card:',document.querySelectorAll('.sp-market-card').length);document.querySelectorAll('.sp-market-card').forEach((c,i)=>console.log('🃏 CARD '+(i+1)+':',clean(c.innerText).slice(0,250)));console.log('🎯 IDs:',{nifty:document.querySelector('#sp-nifty-price'),banknifty:document.querySelector('#sp-banknifty-price'),finnifty:document.querySelector('#sp-finnifty-price'),sensex:document.querySelector('#sp-sensex-price'),vix:document.querySelector('#sp-vix-price')});}
function updateCard(key,price,change){if(price==null||price==='')return 0;const value=fmt(price);let found=0;const sels=['[data-market="'+key+'"] .sp-price','[data-market="'+key+'"] .sp-market-price','[data-price="'+key+'"]','#sp-'+key+'-price','#sp'+key.charAt(0).toUpperCase()+key.slice(1)+'Price'];sels.forEach(sel=>document.querySelectorAll(sel).forEach(el=>{el.textContent=value;el.style.visibility='visible';el.style.opacity='1';found++}));document.querySelectorAll('.sp-market-card').forEach(card=>{const t=clean(card.innerText);const match=(key==='nifty'&&(t.includes('NIFTY 50')||t==='NIFTY'))||(key==='banknifty'&&(t.includes('BANK NIFTY')||t.includes('BANKNIFTY')))||(key==='finnifty'&&(t.includes('FIN NIFTY')||t.includes('FINNIFTY')))||(key==='sensex'&&t.includes('SENSEX'))||(key==='vix'&&(t.includes('INDIA VIX')||t.includes('VIX')));if(!match)return;const candidates=[...card.querySelectorAll('.sp-price,.sp-market-price,.sp-value,.market-price,.price,.value,strong,h2,h3,.elementor-heading-title')];let target=candidates.find(e=>/^(--|—|LOADING|N\/A|0(?:\.0+)?)$/i.test(clean(e.textContent)));if(!target)target=candidates.find(e=>e.children.length===0&&e.textContent.trim().length<30);if(target){target.textContent=value;target.style.visibility='visible';target.style.opacity='1';found++}if(change!=null){const ch=Number(change);const ce=card.querySelector('.sp-market-change,.sp-change,[data-change]');if(ce&&!Number.isNaN(ch))ce.textContent=(ch>0?'▲ +':ch<0?'▼ ':'● ')+ch.toFixed(2)+'%';}const updated=card.querySelector('.sp-updated,[data-updated="'+key+'"]');if(updated)updated.textContent='Updated '+istNow()+' IST';});return found;}
async function load(){try{const r=await fetch(API+'?t='+Date.now(),{cache:'no-store'});if(!r.ok)throw Error('HTTP '+r.status);const j=await r.json();const d=j?.data&&typeof j.data==='object'?j.data:j;for(const k of Object.keys(markets)){const p=markets[k].map(x=>d?.[x]).find(v=>v!=null&&v!=='');const c=markets[k].map(x=>d?.[x+'Change']).find(v=>v!=null&&v!=='');updateCard(k,p,c);}}catch(e){console.error('[STRIKE PULSE] PRICE ERROR',e)}}
function start(){diagnostics();updateMarketStatus();load();setInterval(load,5000);setInterval(updateMarketStatus,1000);console.log('⏱️ AUTO REFRESH: prices 5s | status 1s | updated time IST');}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();
})();